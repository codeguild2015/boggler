import random
rng = random.Random()

dice_throw = rng.randrange(1,7)
dice_throw2 = rng.randrange(1,7)

print('dice_throw', dice_throw)
print('dice_throw', dice_throw2)

"""This script generates random 
outcomes of a simulate dice throw of 2 die"